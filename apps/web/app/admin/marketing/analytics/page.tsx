import { NotBuiltYet } from "../_components/ui";

export default function AnalyticsPage() {
  return (
    <NotBuiltYet
      title="Analytics"
      description="Deep traffic, conversion, and attribution analytics — beyond the Overview KPIs — will live here once the analytics ingest lands."
    />
  );
}
