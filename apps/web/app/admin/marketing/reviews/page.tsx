import { NotBuiltYet } from "../_components/ui";

export default function ReviewsPage() {
  return (
    <NotBuiltYet
      title="Reviews"
      description="Google review monitoring, response drafting, and reputation trends will live here — with high-stakes replies routed through the approval queue."
    />
  );
}
