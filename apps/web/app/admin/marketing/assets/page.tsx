import { NotBuiltYet } from "../_components/ui";

export default function AssetsPage() {
  return (
    <NotBuiltYet
      title="Assets"
      description="Brand assets, photography, and campaign creative — a shared library the agent modules draw from — will live here."
    />
  );
}
