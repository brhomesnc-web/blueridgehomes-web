import { NotBuiltYet } from "../_components/ui";

export default function ContentPage() {
  return (
    <NotBuiltYet
      title="Content"
      description="Blog and page content planning, drafting, and the agent pipeline that proposes posts into the approval queue will live here."
    />
  );
}
